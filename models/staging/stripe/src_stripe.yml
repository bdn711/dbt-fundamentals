version: 2

sources:
  - name: stripe
    database: brett-demo-project
    schema: stripe
    tables:
      - name: payment
        description: Payments made via stripe
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}